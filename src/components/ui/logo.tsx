import { Button } from "./button";

export default function Logo() {
    return (
        <Button variant="ghost" className="hover:bg-transparent hover:cursor-default text-2xl">
            VIMULATUS
        </Button>
    )
}